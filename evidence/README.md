# Evaluation evidence — pending live run

This directory is intentionally a placeholder until the frozen 18-case benchmark is executed with a live
model. `scripts/freeze_evidence.py` replaces this directory from one complete baseline -> ablation -> final
run and refuses to combine mismatched benchmark fingerprints or case sets.

No benchmark improvement claim is published before that happens.
